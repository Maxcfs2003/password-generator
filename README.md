# password-generator

pwgen is a password generator that uses the secrets library 
to generate secure passwords from upper case letters, lower case letters, numbers and special characters.

The minimum number of characters in a password you can genatate is 16,
if the number of characters entered is below 16, a password with 16 characters is generated.
